{
  "name": "flomemos",
  "version": "1.1.0",
  "description": "Flomemos — flomo 风格的轻量级笔记应用，部署在 Cloudflare 免费档（Workers + D1 + R2）",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init-local": "wrangler d1 execute flomemos --local --file=./schema.sql",
    "db:init-remote": "wrangler d1 execute flomemos --remote --file=./schema.sql",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "flomo",
    "memos",
    "notes",
    "cloudflare",
    "workers",
    "d1",
    "r2",
    "free-tier"
  ],
  "license": "MIT",
  "devDependencies": {
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}